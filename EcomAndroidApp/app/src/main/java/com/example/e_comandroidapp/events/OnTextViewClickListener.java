package com.example.e_comandroidapp.events;

//interface for define on click listener when clicked on the category text view
public interface OnTextViewClickListener {
    void  onTextViewClick(String textViewValue);
}
